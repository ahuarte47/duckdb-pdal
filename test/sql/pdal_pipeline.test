# name: test/sql/pdal_pipeline.test
# description: test pdal extension
# group: [sql]

require pdal

query I
SELECT
	COUNT(*)
FROM
	PDAL_pipeline('./test/data/autzen_trim.las', './test/data/autzen-pipeline.json')
;
----
100

query I
SELECT
	COUNT(*)
FROM
	PDAL_pipeline('./test/data/autzen_trim.las', '[ {"type": "filters.tail", "count": 10} ]')
;
----
10
